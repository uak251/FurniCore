name: Frontend Vercel Hardening

on:
  push:
    branches: [main]
    paths:
      - "frontend/furnicore/**"
      - "frontend/furnicore/packages/**"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - ".github/workflows/frontend-vercel-hardening.yml"
  pull_request:
    paths:
      - "frontend/furnicore/**"
      - "frontend/furnicore/packages/**"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - ".github/workflows/frontend-vercel-hardening.yml"

jobs:
  frontend-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install (frozen lockfile)
        run: pnpm install --frozen-lockfile

      - name: Validate env + backend reachability
        env:
          NODE_ENV: production
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
        run: pnpm --filter @workspace/furnicore run check:deploy-env

      - name: Build
        env:
          NODE_ENV: production
        run: pnpm --filter @workspace/furnicore run build

      - name: Test
        env:
          NODE_ENV: test
        run: pnpm --filter @workspace/furnicore run test

      - name: Integration test analytics CSV workflows
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
          LOGIN_TEST_EMAIL: ${{ secrets.RAILWAY_ADMIN_EMAIL }}
          LOGIN_TEST_PASSWORD: ${{ secrets.RAILWAY_ADMIN_PASSWORD }}
        run: |
          if [ -z "$VITE_API_URL" ] || [ -z "$LOGIN_TEST_EMAIL" ] || [ -z "$LOGIN_TEST_PASSWORD" ]; then
            echo "Missing required secrets for analytics CSV integration test."
            echo "Required: VITE_API_URL, RAILWAY_ADMIN_EMAIL, RAILWAY_ADMIN_PASSWORD"
            exit 1
          fi
          pnpm --filter @workspace/furnicore run test:integration:analytics-csv
